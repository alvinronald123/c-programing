#include <stdio.h>
int main (void)
{
  int speed = 0;
  int time = 0;

  printf("speed"); 

    printf("time");
  scanf("%d", &time );

   int distance = speed*time;
   printf("dist: %d\n",distance);
    return 0;
}